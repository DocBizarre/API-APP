# PyInstaller build artifacts
build/
dist/

# Distribution : exécutables compilés + config locale (IP réelle) +
# dossiers générés à l'exécution (bons PDF/HTML, photos clients)
EMS_Distribution/

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd

# Données runtime (DB, logs)
ems_api/data/ems.db
ems_api/data/ems.log
ems_project/data/ems.db
*.log

# Config locale (contient IP du serveur)
# config.ini (racine) est versionné intentionnellement (template)

# Environnement virtuel
venv/
.venv/
env/

# Éditeurs
.vscode/
.idea/
*.swp
*~

# OS
Thumbs.db
.DS_Store
